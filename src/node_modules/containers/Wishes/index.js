import React from 'react'

import { Layout } from 'containers/Layout'
import { wishes } from 'api/mockWishes'
import { WishCard } from 'components/WishCard'

export const Wishes = () => {
    return (
        <Layout>
            { wishes.map((wish, index) => <WishCard wish={ wish } index={ index }/>) }
        </Layout>
    )
}