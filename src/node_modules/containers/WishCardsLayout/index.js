import React, { Component, useState, useEffect } from 'react'
import { useSelector } from 'react-redux'
import * as R from 'ramda'

import { AppLayout } from 'containers/AppLayout'
import { wishes } from 'api/mockWishes'
import { WishCard } from 'components/WishCard'
//import { useCurrentWorkSpaceWidth } from 'getters'



export const WishCardsLayout = () => {
    const columns = useSelector(state => state.wishColumns.wishColumns);
    console.log(columns);
    return (
        <AppLayout>
            {columns.map(column =>
                <div key={column.id} className={'wish-column'}></div>
            )}
        </AppLayout> 
    );
}

//<RenderColumn key={column.id}/>

//{ wishes.map((wish, index) => <WishCard wish={ wish } key={ index }/>) }


