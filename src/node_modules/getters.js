import { useState, useEffect } from 'react'

//export const getWorkSpaceWidth

//export const getColumnsQty


/*
export const useCurrentWorkSpaceWidth = () => {
    const getWorkSpaceWidth = () => document.querySelector('.work-space').clientWidth;
    let [workSpaceWidth, setWorkSpaceWidth] = useState(getWorkSpaceWidth());
    useEffect(() => {
        const resizeListener = () => {
            setWorkSpaceWidth(getWorkSpaceWidth())
        };
        window.addEventListener('resize', resizeListener);
        return () => {
            window.removeEventListener('resize', resizeListener);
        }
    }, [])

    return workSpaceWidth;
}

const outerSpacing = 55;
const innerSpacing = 22;
const columnMinimalWidth = 220;
const columnsQty = Math.floor((useCurrentWorkSpaceWidth() - 2 * outerSpacing + innerSpacing) / (columnMinimalWidth + innerSpacing));

const getColumnHeight = ''

const getShortestColumn = R.reduce((acc, item) => R.prop('height', acc) < R.prop('height', item) ? acc : item, []);
*/

//console.log(getShortestColumn(columns));

