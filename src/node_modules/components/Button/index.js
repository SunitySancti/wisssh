import React from "react"

export const Button = ( props ) => {
    return (
        <button className={props.type + ' ' + props.state}>
            <span>{props.text}</span>
        </button>
    );
}