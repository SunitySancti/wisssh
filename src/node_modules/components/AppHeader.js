import React from 'react'
import { NavLink } from 'react-router-dom'

import { Icon, LogoIcon } from 'components/Icon'

export const AppHeader = () => {
    return (
        <div className='app-header'>
            <div className='logo'>
                <NavLink to="/" className='logo-btn'>
                    <LogoIcon/>
                </NavLink>
            </div>
            <NavLink to="/" className={({isActive}) => isActive ? 'header-nav active' : 'header-nav inactive'}></NavLink>
        </div>
    );
}