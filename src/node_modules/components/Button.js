import React from "react"

export const Button = ({ type, state, text, handleClick }) => {
    return (
        <button
            className = {type + ' ' + state}
            onClick = {handleClick}
        >
            <span>{text}</span>
        </button>
    );
}