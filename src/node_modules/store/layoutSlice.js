import { creactSlice, current } from '@reduxjs/toolkit'

const layoutSlice = creactSlice({
    name: 'layout',
    initialState: {
        appWidth: {
            current: 0,
            lowerThreshold: 824,
            upperThreshold: 1000,
        },
        appHeaderScale: {
            current: 1,
            max: 1,
            min: 0.5,
        },
        appBodySidePaddings: {
            current: 110,
            max: 110,
            min: 22,
        }
    reducers: {
        updateAppWidth(state) {
            const appBody = document.querySelector('.app-body');
            state.appWidth = appBody.clientWidth;
        },
        updateLayoutStyles(state) {
            const range = current(state).appWidth.upperThreshold - current(state).appWidth.lowerThreshold;

        }

    }
})